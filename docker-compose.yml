services:
  panel:
    build: .
    ports:
      - "${PANEL_PORT:-8443}:8443"
    volumes:
      - ./data:/app/data
      - /usr/local/etc/xray:/etc/xray:ro
      - /etc/hysteria:/etc/hysteria:ro
      - /etc/mita:/etc/mita:ro
    env_file: .env
    restart: unless-stopped
    network_mode: host
